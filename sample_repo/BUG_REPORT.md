# 订单取消后优惠券无法再次使用

## 业务反馈

客服反馈，用户使用优惠券下单后取消订单，再次下单时系统提示优惠券已使用。

## 复现步骤

1. 创建优惠券 `NEWUSER20`。
2. 用户在订单 `ORD-1001` 中使用该优惠券并支付。
3. 调用取消订单接口，请求体为 `{"order_id": "ORD-1001"}`。
4. 查看优惠券状态，或尝试在新订单中再次使用。

## 实际结果

订单状态已变为 `cancelled`，优惠券仍为 `used`，用户无法再次使用。

## 预期结果

订单取消成功后，优惠券应恢复为 `available`，可用于后续订单。
